import React from "react";
import { StoryAdConfigForm } from "./StoryAdConfigForm";

// For now, just re-export the same component. You can refactor this for Choice Point specifics later.
export default StoryAdConfigForm; 